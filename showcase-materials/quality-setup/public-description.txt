Quality Setup is a local, preview-first inspector for repository quality setup. It detects a supported Node target, previews one owned configuration change, applies only after explicit authority, verifies the result, refuses ambiguous overwrite, and leaves a rollback receipt. The prototype keeps installation local, bounded, and recoverable instead of treating command dispatch as proof.
