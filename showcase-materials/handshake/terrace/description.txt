Terrace makes an agent workflow recoverable after a real stop. In the representative routing regression, an active gpt56-modernization workflow was preserved instead of being replaced by an unrelated roadmap phase. The integrated workflow stores stage state, records an attributable stop packet, rejects unsupported bypasses, and resumes only the blocked stage after a bounded correction. The visual package is local; hosted publication is not claimed.
