Context Compiler Contract validates agent context bundles at the contract boundary. A real AIOS compile result passes; removing one source-selection reason and flipping route_compatible to false produce exact field-level failures; restoring both returns the same digest. The package proves deterministic validation. AIOS remains responsible for context selection and downstream execution.
