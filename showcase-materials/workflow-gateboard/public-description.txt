Workflow Gateboard reads repository-declared checks and makes prerequisites, mutation class, freshness, and receipts visible. Its current dev checkpoint writes a syntax receipt and keeps dependent tests/package blocked until prerequisite state is handed off; the historical W2 fixture records the intended full read-only chain. The board leaves a continuation receipt instead of treating command dispatch as proof of hosted CI.
