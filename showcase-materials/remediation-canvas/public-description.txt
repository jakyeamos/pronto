Remediation Canvas groups stable Quality Lens finding references with human intent and explicit dispositions. The current dev checkpoint preserves partial work when one finding is resolved, another is blocked, and the source set later changes: refresh marks the canvas stale instead of silently carrying a false plan. It references authority rather than copying the finding payload; downstream review integration remains open.
