# RDW Agent Prompt Bundle

This bundle does not call an LLM by itself. Give it to an agent with repo/file/web tools.

## Task Contract

```yaml
task_id: rdw-tatum-claim-boundary
task:
  Demonstrate RDW rejecting and repairing the unsupported claim that Jayson Tatum's
  2023-24 box-score line proves he was an elite two-way engine.
domain: basketball
pack_exists: true
pack_readiness:
  schema_version: rdw-domain-pack/v1
  domain: basketball
  kind: undeclared
  maturity: undeclared
  ok: false
  errors:
    - pack_contract.schema_version must be rdw-domain-pack/v1
    - pack_contract.kind must be specialized|fallback|scaffold
    - pack_contract.maturity must be production|baseline|scaffold
  warnings: []
  metrics:
    research_question_count: 0
    qa_check_count: 0
    concept_count: 0
    jargon_count: 0
    declared_output_type_count: 0
    template_output_type_count: 0
    acceptance_positive_count: 0
    acceptance_negative_count: 0
    acceptance_ambiguous_count: 0
    production_output_types: []
    acceptance_output_types: []
entity_type: player
entity_name: Jayson Tatum
topic:
  "stat_interpretation: Demonstrate RDW rejecting and repairing the unsupported
  claim that Jayson Tatum's 2023-24 box-score line proves he was a"
output_type: stat_interpretation
output_format: markdown
audience: AI product and sports analytics evaluators
research_needed: true
research_depth: standard
research_requirements:
  schema_version: rdw-research-requirements/v1
  domain: basketball
  output_type: stat_interpretation
  research_depth: standard
  required_questions:
    - id: basketball-sample
      question: What population, season, sample, and freshness window bound the analysis?
      dimension: sample
      consequence: consequential
    - id: basketball-role-context
      question: What role, usage, team, and lineup context shape the numbers?
      dimension: context
      consequence: consequential
    - id: basketball-metric-mechanism
      question: What do the cited metrics capture and miss?
      dimension: mechanism
      consequence: consequential
    - id: basketball-strengths-limits
      question: What strengths and limitations are supported by the evidence?
      dimension: limits
      consequence: consequential
    - id: basketball-counterread
      question: What plausible comparison or skeptical reading constrains the conclusion?
      dimension: counterread
      consequence: supporting
  required_synthesis:
    - thesis
    - limitations
    - examples
    - counterevidence
  minimum_coverage_ratio: 1.0
packet_id: basketball-player-jayson-tatum-2023-24-showcase
packet_resolution:
  schema_version: rdw-packet-resolution/v1
  packet_id: basketball-player-jayson-tatum-2023-24-showcase
  packet_path: knowledge/basketball/basketball-player-jayson-tatum-2023-24-showcase.yaml
  status: missing
  action: create_candidate
  exists: false
  can_reuse: false
  reasons:
    - canonical packet not found
  warnings: []
  packet_validation:
    ok: false
    errors: []
    warnings: []
  research_sufficiency:
    schema_version: 1
    status: not_evaluated
    writing_material_ready: false
    blockers: []
  next_safe_step:
    Research the task requirements, write a run-local candidate packet,
    then promote it through the parent-head gate.
inference:
  mode: mixed
  confidence: low
  fields_inferred:
    - output_format
  fields_explicit:
    - domain
    - entity_name
    - output_type
    - audience
    - research_depth
    - packet_id
    - task_id
  rationale:
    Contract generated by rdw's deterministic router; agent may refine before
    writing.
local_knowledge_paths:
  - knowledge/basketball/basketball-player-jayson-tatum-2023-24-showcase.yaml
qa_checklist_path: domains/basketball/qa-checklist.md
writing_template: domains/basketball/writing-templates.md
style_profile_path: config/style-profile.yaml
warnings:
  - no domain signals matched; defaulted to general
  - "domain pack is not ready for its declared maturity: pack_contract.schema_version
    must be rdw-domain-pack/v1; pack_contract.kind must be specialized|fallback|scaffold;
    pack_contract.maturity must be production|baseline|scaffold"
```

## Execution Order

1. Read `SKILL.md` and the prompts listed below.
2. Confirm or adjust the task contract if the user objects.
3. Run research, knowledge packet, draft, QA, and humanizer in order.
4. Save artifacts in the `output_format` from the contract above, using the output paths in `config/output-formats.yaml`.

## Router Prompt

# Domain Router

Infer the full task contract from natural language. **The user does not need to pass `domain=`, `entity=`, etc.** — only when they want to override.

## Inputs

- User writing request (raw) — may be a single sentence
- Optional explicit overrides (`domain=`, `entity=`, `output-type=`, `audience=`, `depth=`, `packet-id=`)
- `config/domains.yaml`
- `config/router-inference.yaml`
- List of available packs under `domains/`
- Optional: project paths, files, or URLs the user mentions (load for entity context)

## Outputs (YAML)

```yaml
# Original user text (always)
task: string

# Inferred or overridden — report all of these to the user before pipeline continues
domain: string
pack_exists: boolean
pack_readiness:
  schema_version: rdw-domain-pack/v1
  kind: specialized | fallback | scaffold
  maturity: production | baseline | scaffold
  ok: boolean
  errors: []
  warnings: []
entity_type: string
entity_name: string
topic: string
output_type: string
audience: string
research_needed: boolean
packet_resolution:
  schema_version: rdw-packet-resolution/v1
  status: missing | refresh_required | review_required | reusable
  action: create_candidate | refresh_candidate | resolve_review | reuse
  exists: boolean
  can_reuse: boolean
  reasons: []
  next_safe_step: string
research_depth: light | standard | deep

# Transparency
inference:
  mode: inferred | explicit | mixed
  confidence: high | medium | low
  fields_inferred: []
  fields_explicit: []
  rationale: string # one short paragraph

local_knowledge_paths: []
qa_checklist_path: string
writing_template: string
style_profile_path: config/style-profile.yaml
warnings: []
```

## Inference procedure (required)

1. **Start from** `config/router-inference.yaml` defaults.
2. **Parse user text** for domain signals (jargon, surface names like "LIS leaderboard", product names).
3. **Apply** `domain_inference` keyword lists → pick domain with most signals; if tie, prefer basketball only when sports/stat cues present.
4. **Apply** `output_type_inference` and `entity_inference` patterns (e.g. "leaderboard" → `ranking_explanation`, entity `LIS leaderboard` if named).
5. **Apply** `audience_inference` match lists.
6. **Apply** `depth_inference`: deep triggers win; light triggers win; else `standard`.
7. **Explicit overrides** from `key=value` or user saying "domain is X" → replace inferred field; set `mode: mixed`.
8. **Entity naming**: preserve user’s proper nouns (LIS, product names) verbatim in `entity_name`.
9. **Topic**: short phrase for what the writing must accomplish (e.g. "improve leaderboard UI copy").
10. **Do not invent domain facts** — only route metadata. Unknown domain → `general` + warning.
11. **Do not equate presence with maturity** — `pack_exists` only proves resolution.
    Surface `pack_readiness`; a specialized pack with `ok: false` cannot support
    production drafting until its deterministic pack gate passes.
12. **Do not equate packet presence with reusability** — use the deterministic
    `packet_resolution` projection. Only `action: reuse` may skip research. Missing,
    stale, incompatible, insufficient, legacy, conflicted, or review-required packets
    must create/refresh a candidate or stop for review as projected.

## Worked example

**User:** "I want you to improve the copy on my LIS leaderboard."

```yaml
task: Improve copy on the LIS leaderboard (headers, row blurbs, methodology as needed).
domain: basketball
entity_type: ranking
entity_name: LIS leaderboard
topic: Leaderboard UI and ranking copy grounded in what LIS measures
output_type: ranking_explanation
audience: fantasy and analytics users who know basic stats
research_depth: standard
research_needed: true
inference:
  mode: inferred
  confidence: medium-high
  fields_inferred:
    [domain, entity_type, entity_name, output_type, audience, research_depth]
  rationale: LIS + leaderboard implies basketball analytics ranking surface; not a single-player task unless user narrows.
```

## After routing

- **Show the inferred contract** to the user in a compact table; proceed unless they object.
- Continue pipeline without requiring them to re-type parameters.

## Rules

- Prefer reusing `knowledge/<domain>/*.yaml` when packet id matches entity (e.g. `basketball-lis-leaderboard` if exists).
- Reuse that path only when `packet_resolution.action: reuse`; otherwise preserve the
  current head and follow `next_safe_step`.
- Flag `warnings` when `inference.confidence` is low.
- Never block the run solely because parameters were omitted.

## Pipeline Orchestrator

# Pipeline Orchestrator (Single Task)

Run the full research-domain-writing pipeline for one task.

## Research & batch

- **Research:** agent executes `researcher.md` (may use any tools); skill saves structured packets (`docs/LIMITATIONS.md`).
- **Batch (v0.1):** `rdw batch plan <batch.yaml>` creates task bundles; agent follows `prompts/batch-runner.md` to execute them.

## Load order

1. `config/domains.yaml`, `config/router-inference.yaml`, `config/style-profile.yaml`, `config/output-formats.yaml`
2. `prompts/domain-router.md` → router output (**infer** domain, entity, output_type, audience, depth from user text if omitted)
3. Present inferred contract to user; proceed without requiring `key=value` args
4. For a specialized pack, require `pack_readiness.ok: true` (or run
   `rdw validate-pack <domain>`). `pack_exists` alone is insufficient, and only
   acceptance-backed `production_output_types` may claim mature support.
5. Enforce `packet_resolution.action` before research or drafting:
   - `reuse`: skip research and use the accepted head;
   - `create_candidate`: research a new run-local candidate;
   - `refresh_candidate`: preserve the head and research a child candidate; or
   - `resolve_review`: stop confident drafting until explicit resolution.
6. If research needed: `prompts/research-planner.md` → `prompts/researcher.md`
7. `prompts/knowledge-packet-builder.md`
8. For create/refresh, promote the candidate with `rdw packet promote`, then regenerate
   the task contract and require `packet_resolution.action: reuse`.
9. `rdw research-readiness <packet> --contract <task-contract> --json` — separate
   source, packet, output, and output-specific research sufficiency. Continue research
   until `writing_material_ready` and `can_draft_confidently` are true.
10. `prompts/domain-copywriter.md`
11. `prompts/domain-qa.md` — emit the exact-draft hash, universal prose-block claim
    ledger, and stable rule results; run `rdw validate-claim-ledger ... --draft ...`
12. Compare packet/draft against an approved diff baseline; only `pass` may enter `qa-passed`
13. `prompts/humanizer-blader.md`
14. Save artifacts per `config/output-formats.yaml`

## Artifact map

| Stage               | Path                                                                            |
| ------------------- | ------------------------------------------------------------------------------- |
| Research packet     | `knowledge/<domain>/<id>.yaml`                                                  |
| Candidate packet    | `outputs/research/<id>.yaml`                                                    |
| Conflict/resolution | `outputs/research/<id>-conflict*.yaml`, `outputs/research/<id>-resolution.yaml` |
| Knowledge packet    | `outputs/research/<task_id>-knowledge.md`                                       |
| Draft               | `outputs/drafts/<output_id>.md`                                                 |
| QA                  | `outputs/qa/<output_id>-qa.yaml`                                                |
| Claim ledger        | `outputs/qa/<output_id>-claims.yaml`                                            |
| Diff QA             | `outputs/qa/<output_id>-diff.yaml`                                              |
| Readiness           | `outputs/qa/<output_id>-readiness.yaml`                                         |
| Final               | `outputs/final/<output_id>.md`                                                  |

## Principle

**Knowledge stages may add facts. Humanizer may not.**

## User invocation (agent)

Provide in one message:

```
Task: <writing request>
Domain (optional): <domain>
Entity: <name>
Output type: <type>
Audience: <audience>
Research depth: light|standard|deep
Existing packet (optional): <packet_id>
```

Then execute orchestrator steps in order without skipping QA before humanizer.
Lifecycle promotion verifies the canonical packet, draft, QA/claim ledger, readiness,
and final artifacts; status names alone cannot stand in for missing evidence.
