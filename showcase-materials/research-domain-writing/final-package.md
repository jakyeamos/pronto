# Research Domain Writing publication candidate

The agent-owned package is assembled around the real Jayson Tatum 2023–24 case.
The public surface shows the source packet, the tempting overclaim, the precise
QA stop, the evidence-safe repair, and the human publication boundary.

## Included

- `public/index.html` — responsive, interactive claim-trace candidate
- `public-description.txt` — short Showcase copy
- `captions.vtt` — draft captions for the target narration
- `case-study.json` and `claim-ledger.json` — reviewed public claim boundary
- `source-packet.yaml` — real two-source evidence packet
- `rdw-run/` — unsafe and repaired QA receipts plus the final prose
- `rdw-0-provenance.json` — installed-pack and source-revision disposition

## Honest boundary

The installed basketball pack validates as `specialized/production`, and its
domain contract is byte-identical to repository revision `aa46b36`. The wheel
does not embed that revision, its recorded build worktree no longer exists, and
the active source checkout differs. The page is therefore a publication
candidate, not release-provenance proof or a deployed public case study.

## Local surface verification

External Chrome rendered the candidate at `1488x981` and `390x844`. Both
layouts had no horizontal document overflow. The desktop claim selector changed
the visible receipt from the supported season line to the stopped impact claim,
with no console warnings or errors. The first mobile pass exposed a clipped
third claim tab; the selector was changed to a vertical phone layout and all
three states then rendered within the viewport.

## Remaining human and external gates

1. Select and attest or rebuild the intended release revision.
2. Complete the side-by-side editorial review of the repaired prose.
3. Record the final human delivery and retime captions to it.
4. Host the page without authentication and verify desktop and mobile.
5. Approve the final public copy.
