# Project Truth

## Current State

- The agreed PRD-complete V1 local/read-only Pronto desktop boundary is implemented on `main`.
- Tauri + React/TypeScript provides the desktop shell and portfolio console.
- Native Rust discovers repositories and linked worktrees, scans structured Git state, persists snapshots, transition events, and action audits, and exposes Tauri plus read-only CLI commands.
- The repository-selection Git environment is sanitized before native Git calls so hook variables cannot redirect scans or fixtures.
- A canonical PRD behavior inventory, coverage boundary sheet, delivery plan, and verification matrix are committed at `docs/pronto-behavior-spec.xlsx`; the workbook tracks 36 behaviors through implementation snapshot `c0bf758`, with 22 regression-covered, 5 verified/build-verified, 9 environment-gated, zero unresolved product decisions, and zero formula errors after a three-sheet visual review.
- The local desktop surface now has truthful navigation boundaries, Activity and Settings views, Cmd/Ctrl-K search focus, Escape drawer dismissal, freshness copy, and distinct filtered-empty states.
- Local durable state now uses a versioned SQLite database with non-destructive import from the legacy JSON registry; renderer and CLI snapshot contracts are unchanged.
- Safe local refresh/inspect preflights now use an explicit allowlist, exact root/repository target IDs, blocked-action records, and durable SQLite audit history; no destructive Git or provider-write action is exposed.
- Local portfolio configuration is now durable and editable: discovery-root ignore/fetch/monitoring settings, explicit product and group membership, product release modes, repository lifecycle confirmation, retention days, and recursive submodule summaries are stored in SQLite schema v5 and surfaced in the desktop UI.
- The companion CLI now shares the local registry and action boundary for discovery-root registration, append-only discovery exclusions, product/group status filters, scoped read-only refreshes, authenticated GitHub provider refreshes, maturity-audit configuration, structured JSON output, repository/app opening, and an audited blocked clone command; `pronto root add <folder>` configured `/Users/jakyeamos/projects`, `pronto root exclude <folder> <name>...` configured `not-mine` and `test-fixtures`, `pronto refresh-github` reached Ready for the configured portfolio, and `pronto quality audit-root <folder|clear>` now persists the external maturity source.
- The Tauri bundle explicitly selects the desktop `pronto` binary when the repository also contains `pronto_cli`; the current release is installed at `/Applications/Pronto.app` and launches successfully.
- The default 800x600 desktop window now removes shell-level horizontal overflow, uses a compact two-column metric layout, stacks the dashboard rail, and reflows secondary grids for narrower surfaces; a direct capture of the installed window verified the compact dashboard rendering.
- The full live visual pass now keeps the quality matrix readable at compact size with sticky repository/maturity/findings context, bounded gate columns, an explicit horizontal comparison hint, and wrapped shared surface headers; primary navigation, quality, detail, settings, and error states were reviewed in the installed bundle.
- A provider-neutral remote snapshot contract now persists GitHub identities, only remote repositories linked to in-scope local checkouts, locality matches, freshness, and unavailable states in SQLite schema v5; the desktop Remote catalog is read-only and never stores provider credentials.
- Workspaces now expose provider-neutral agent activity signals from process metadata and optional `.pronto/agent.json` manifests, with explicit `Active`, interrupted, recently active, and `Unknown` states; active evidence blocks integration eligibility without capturing terminal contents or command arguments.
- Workspace detail cards now provide permission-safe external handoff buttons for the exact registered workspace, opening Finder, Terminal, Visual Studio Code, or GitHub Desktop through structured macOS `/usr/bin/open` arguments without staging, committing, or changing registry state.
- Repository detail now offers a read-only preparation preview for pull requests and releases: exact head/base/commit/push/cleanliness evidence, provider PR/check/review uncertainty, published release baselines, deterministic conventional-commit grouping, and a candidate version are shown without creating a branch, worktree, commit, push, PR, or release.
- Repository release preparation now supports locally persisted deterministic rules with normalized AND/OR operators, commit-count/elapsed-time/conventional-type clauses, explicit first-release handling, exact neutral threshold wording, and passed/failed/unknown trace rows; saving a rule remains local-only.
- Quality evidence is now a read-only domain: the Quality Gates matrix shows eight canonical gates—Build, Smoke, Tests, Lint, Formatter, Typecheck, Dead-code, and Secrets scan—while Dependency audit appears conditionally when evidence applies; discovered custom gates remain opt-in in the matrix and available in repository detail and release-rule configuration. CI, local, and QR evidence preserves timestamps, commits/branches, freshness, severity counts, constrained report paths, and exact imported maturity values.
- CI readiness is now a separate Pronto-derived score: six baseline gates plus applicable conditional gates must have fresh passing evidence for a repository to reach `4.0`; missing, stale, failed, and blocked gate IDs are exposed per repository and in the fleet summary without changing imported maturity values.
- Quality evidence is persisted with store v4 / SQLite schema v5 compatibility defaults; QR run manifests and reports, GitHub check runs, optional audit roots, repository matching, attention filtering, repository detail, and explainable release-rule gate/source traces are covered without executing commands.
- A read-only project recommendation matrix now covers all 57 records in the live portfolio snapshot, separates baseline from conditional gates, identifies raw CI labels for taxonomy removal, and marks 44 available paths, 13 unavailable paths, and four dogfood fixture candidates without guessing evidence.
- A configured release rule now materializes the exact `Configured release threshold met` queue condition only when provider context is Ready and the deterministic trace passes; stale threshold conditions are removed when evidence is unavailable or fails.
- Repository preparation now exposes a local AI summary payload preview with repository permissions `Disabled`, `Commit metadata only`, and `Committed diff allowed`; payload bytes, categories, committed source references, provider/model trace fields, and an explicit no-request state are visible, and uncommitted content is excluded.
- Repository preparation now includes a validated release recipe/version-confirmation preview: candidate confirmations must match the current deterministic candidate, recipe commands and generated paths are persisted locally, and the nine-step worktree/script/commit/push/PR/release sequence remains preview-only.
- The portfolio-wide local refresh now requires an explicit confirmation: the dialog states its repository/root scope, warns that a large scan can take minutes, explains that it reads existing evidence only, supports cancel/Escape, and keeps a visible scanning state while the refresh is running.
- The local refresh scan now runs on Tauri's blocking worker pool instead of the UI thread, so the window can repaint and keep the in-progress state visible during the read-only scan.
- A full local refresh now prunes repositories whose registered paths no longer exist instead of retaining stale `Local path unavailable` records; the live store was refreshed to 44 current repositories with zero `not-mine` or `test-fixtures` paths, and the command center now surfaces imported fleet maturity beside derived CI readiness.
- Native persistence now records deduplicated analytics samples with retention pruning, provider-neutral connection/workflow records, adapter status, review state, and redacted command evidence without executing discovered commands.
- The remediation domain now has a versioned `pronto-remediation/v1` contract, scoped refresh steps, QR finding/maturity/CI-ideal evidence, per-repository plans, action status transitions, and JSON export; `soundscape` and `tenure` are explicit exclusions.
- The desktop renderer now exposes analytics charts, evidence-backed connections and workflow review, portfolio collections, per-repository remediation plans, action status updates, and remediation export while preserving explicit empty, unavailable, and freshness states.
- The implementation plan documents the focused read-only CLI projections, and the quality-gate matrix documents configured ideal gates separately from fresh passing evidence, including Pronto's own row.

## Current Position

- Branch: `main`
- Implementation and documentation commits: `1817527`, `ef6ea99`, `73c28e5`, `533c2f5`, `885fe1e`, `faf0d99`, `d332573`, `f01c627`, `b13de08`, `5a0b120`, `42e8b54`, `853faa6`, `ed8d05f`, `6fde71e`, `8be4771`, `75889f3`, `3ad1451`, `6ed50bb`, `c3c9391`, `c0bf758`, `6a3cd19`, `2b0a0db`, `57501b5`, `4fb3083`, `e58e91c`, `9ece39f`, `39d5b80`, `865b35a`, `16356df`, `98d0d39`, `a456445`, `6026602`, `c0597b1`, `7e734f1`, `fb5ea92`, `88e1a83`, `c4e9a46`, `4c73ee5`, `360faf2`, `05948ec`, `a0512d1`, `659b1ae`, `ad93d98`
- Verification for `3ad1451`: 19 Rust tests, offline cargo check/test, cargo fmt, typecheck, lint, Prettier, renderer production build, `git diff --check`, and the source commit-time Pre-CR gate all passed.
- Verification for `6ed50bb`: 20 Rust tests, offline cargo check/test, cargo fmt, typecheck, lint, Prettier, renderer production build, `git diff --check`, and the source commit-time Pre-CR gate all passed.
- Verification for `c0bf758`: 20 Rust tests, `pnpm test` (including the no-test Vitest pass-through), offline cargo test/check, cargo fmt, typecheck, lint, source Prettier, renderer production build, full Tauri app/DMG build, `git diff --check`, and the source commit-time Pre-CR gate all passed; the refreshed workbook also passed zero-error inspection and three-sheet visual review.
- Verification for `4fb3083`: 30 Rust tests, `cargo test`, `pnpm lint`, `pnpm typecheck`, `pnpm format:check`, `pnpm test`, `pnpm build` with full Tauri app/DMG bundles, `git diff --check`, Impeccable UI detection, and the source commit-time Pre-CR plus user quality gates all passed.
- Verification for `e58e91c`: `cargo fmt --check`, 31 Rust tests, `pnpm typecheck`, `git diff --check`, source commit-time gates, and a successful `pnpm cli root add /Users/jakyeamos/projects --json` scan with a completed read-only refresh audit.
- Verification for `9ece39f`: `cargo fmt --check`, 31 Rust tests, authenticated `gh auth status`, read-only `gh api user`, and `pnpm cli refresh-github` all passed; the provider reported Ready and refreshed 59 repositories.
- Verification for `39d5b80`: `pnpm build` produced a release bundle whose executable is `pronto`, the bundle was installed at `/Applications/Pronto.app`, and `open /Applications/Pronto.app` succeeded; Spotlight validation was unavailable because `mdutil` reports the Spotlight server disabled in this execution environment.
- Verification for `865b35a`: Cargo metadata reports `default_run: pronto`, `pnpm build` selected the desktop target, direct launch no longer emitted CLI status output, and the installed `/Applications/Pronto.app` remained a live process after `open`.
- Verification for `16356df`: `pnpm lint`, `pnpm typecheck`, stylesheet Prettier, `pnpm test` (31 Rust tests and 5 Vitest tests), `pnpm build` with full Tauri app/DMG bundles, `git diff --check`, Impeccable UI detection, and a direct capture of the installed 800x600 window all passed; the truth snapshot was normalized before the final repository-wide format check, which also passed.
- Verification for `98d0d39`: `cargo test` (31 Rust tests), `pnpm lint`, `pnpm typecheck`, `pnpm format:check`, `pnpm test` (31 Rust tests and 5 Vitest tests), `pnpm build` with full Tauri app/DMG bundles, `git diff --check`, Impeccable detection, and installed-bundle captures at 800x600 all passed; the refreshed release bundle was copied into `/Applications/Pronto.app` and the primary navigation, quality matrix, repository detail, settings, and error state were reviewed.
- Verification for `a456445`: 32 Rust tests, `pnpm lint`, `pnpm typecheck`, `pnpm format:check`, `pnpm test` (32 Rust tests and 5 Vitest tests), `pnpm build` with full Tauri app/DMG bundles, the `pronto root exclude /Users/jakyeamos/projects not-mine test-fixtures --json` refresh, zero matching local repository paths in the SQLite registry, and a running `/Applications/Pronto.app` all passed.
- Verification for `6026602`: 32 Rust tests, `pnpm test` (32 Rust tests and 5 Vitest tests), `pnpm lint`, `pnpm typecheck`, `pnpm format:check`, `cargo fmt --check`, `git diff --check`, a full Tauri app/DMG build, installation and relaunch of `/Applications/Pronto.app`, and a live CLI snapshot showing only 49 connected remote records all passed.
- Verification for `c0597b1`: 32 Rust tests, 5 Vitest tests, `pnpm lint`, `pnpm typecheck`, `cargo fmt --check`, full Tauri app/DMG build, installation and relaunch of `/Applications/Pronto.app`, and live maturity configuration from `/Users/jakyeamos/projects/ai-workflow-leverage/artifacts/audit` all passed; the latest imported run is `audit_289de0bc91af1144`, with 49 matched repositories and fleet mean `1.719 / 4`.
- Verification for `7e734f1`: 32 Rust tests, 5 Vitest tests, `pnpm lint`, `pnpm typecheck`, Prettier, full Tauri app/DMG build, installation of the final `/Applications/Pronto.app`, `git diff --check`, and the source commit-time Pre-CR plus user quality gate all passed; the refresh confirmation explains scope and read-only behavior before any portfolio-wide scan.
- Verification for `fb5ea92`: 32 Rust tests, 5 Vitest tests, `cargo fmt --check`, `pnpm lint`, `pnpm typecheck`, full Tauri app/DMG build, installation of the final `/Applications/Pronto.app`, `git diff --check`, and the source commit-time Pre-CR plus user quality gate all passed; the local refresh now runs off the UI thread.
- Verification for `88e1a83`: 32 Rust tests, 5 Vitest tests, `pnpm lint`, `pnpm typecheck`, `pnpm format:check`, full Tauri app/DMG build, installation of the final `/Applications/Pronto.app`, `git diff --check`, and the source commit-time Pre-CR plus user quality gate all passed; the fleet matrix renders only the six canonical gates and never adds discovered custom columns.
- Verification for `c4e9a46`: 32 Rust tests, 5 Vitest tests, `pnpm lint`, `pnpm typecheck`, `pnpm format:check`, `git diff --check`, full Tauri app/DMG build, installation of the final `/Applications/Pronto.app`, and the source commit-time Pre-CR plus user quality gate all passed; Tests and Secrets scan are canonical, Dependency audit is conditional, and absent conditional/custom cells remain visually empty.
- Verification for `4c73ee5`: the recommendation report contains one row for each of the 57 live snapshot records, was generated without tests, audits, or network refreshes, and passed Prettier, `pnpm format:check`, and `git diff --check`.
- Verification for `360faf2`: 36 Rust tests, `pnpm lint`, `pnpm typecheck`, `pnpm format:check`, `pnpm test` (36 Rust tests and 5 Vitest tests), `cargo fmt --check`, `git diff --check`, and a full Tauri app/DMG build all passed; the readiness score preserves imported maturity and identifies open gate updates.
- Verification for `05948ec`: 38 Rust tests, 6 Vitest tests, `pnpm lint`, `pnpm typecheck`, `pnpm format:check`, targeted Prettier checks, `cargo fmt --check`, `git diff --check`, a full Tauri app/DMG build, installation of `/Applications/Pronto.app`, the source commit-time Pre-CR gate, and a live `pnpm cli refresh` all passed; the live registry reports 44 repositories, imported maturity `1.513 / 4`, derived CI readiness `0.0 / 4`, and zero excluded-path records.
- Next deliverable: record the final implementation-plan and CI-matrix documentation state, run the complete repository quality ladder, and publish the verified feature set; live folder-picker and data-dependent provider/preparation environment behavior remain the only verification boundaries.

## Blockers

- Live GitHub read-only refresh is authenticated as `jakyeamos` and last saw 59 repositories; only 49 with matching in-scope local checkouts are surfaced, while remote clone/import, modifying action execution, provider-backed pull requests/releases, publication, and actual opt-in AI requests remain explicit deferred boundaries from the implementation plan. The local AI payload preview does not contact a model endpoint.

## Risks

- Folder-picker and some data-dependent preparation calls still need live environment verification; the installed bundle launches successfully and the primary visual surfaces were reviewed, while Spotlight indexing could not be checked because the Spotlight server is disabled in this execution environment.
- Discovery exclusions apply to local repositories under registered roots; GitHub refresh now applies the same local scope to the Remote catalog and omits remote-only snapshots.
- The SQLite schema is now at version 5 with explicit migrations for action audits, root/product/group configuration, provider snapshots, and quality summary metadata; future schema migrations still need explicit migration steps rather than silent compatibility assumptions.
- Provider refresh uses the existing GitHub CLI credential path and persists only sanitized identity/repository snapshots; live authenticated evidence is now present, while check-run/repository detail may still be incomplete or unavailable for individual remotes and mutation/publication remains deferred.
- GitHub pull-request and release detail refreshes are read-only and may leave checks, reviews, mergeability, or release baselines unknown when provider detail calls are unavailable; the UI preserves that uncertainty instead of inferring readiness.
- Release rules currently cover deterministic numeric and conventional-commit clauses with flat AND/OR composition; repository-specific scripts/predicates and nested grouped conditions still require a separately audited design.
- Release recipes validate command/path metadata and show a nine-step read-only preflight, but do not execute scripts, create worktrees, generate files, commit, push, open pull requests, or publish releases.
- AI is currently a local preview/trace surface only; endpoint/provider credential configuration and user-approved request execution remain intentionally unimplemented until the external-permission boundary is explicitly reopened.
- Process inspection is intentionally metadata-only and may report `Activity state uncertain` when OS permissions or platform support prevent working-directory correlation.
- External handoff currently targets macOS and fixed application names; live desktop launch proof and configurable/non-macOS tool mappings remain open.
- Action audit identifiers and transition event identifiers use process-local sequences to avoid same-second collisions; cross-process concurrency is not yet a supported store mode.
- Pre-CR is configured with a zero-threshold no-instrumentation adapter; functional tests, typechecks, and builds are the evidence gates until a real coverage instrumenter is added.

## Recent Progress

| Date       | Change                                                                                                                                                                                                    | Evidence                                                                                                    |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| 2026-07-27 | Documented the focused CLI projections and separated configured ideal CI gates from fresh passing evidence in the recommendation matrix.                                                               | `ad93d98`; doc-only commit quality gate passed                                                               |
| 2026-07-27 | Added analytics charts, evidence-backed connections/workflows, portfolio collections, remediation plans, action status controls, export, and focused renderer behavior tests.                             | `659b1ae`; commit-time Pre-CR and user quality gate passed                                                   |
| 2026-07-27 | Added persisted analytics, connection/workflow, and evidence-backed remediation refresh state with QR/maturity/CI-ideal ingestion and Soundscape/Tenure exclusions.                                   | `a0512d1`; commit-time Pre-CR and user quality gate passed                                                   |
| 2026-07-26 | Made the default 800x600 desktop window responsive, removing horizontal clipping and stacking compact dashboard content; installed and visually verified the rebuilt app.                                 | `16356df`; UI gates, Tauri build, and direct window capture passed                                          |
| 2026-07-26 | Completed the full installed-bundle visual pass: shared headers stay readable, the quality matrix uses bounded columns and horizontal comparison, and primary detail/settings/error states were reviewed. | `98d0d39`; detector clean, all gates passed, 800x600 captures reviewed                                      |
| 2026-07-26 | Added case-insensitive `pronto root exclude` support, pruned previously registered ignored repositories, and configured `/projects` to exclude `not-mine` and `test-fixtures`.                            | `a456445`; 32 Rust tests, full build, live registry and app verification passed                             |
| 2026-07-26 | Required GitHub remote catalog entries to match an in-scope local checkout and removed remote-only records from loaded and refreshed snapshots.                                                           | `6026602`; all gates, full build, installed app, and 49-record live snapshot passed                         |
| 2026-07-26 | Added `pronto quality audit-root <folder/clear>` and connected the known external maturity-audit source in the local registry.                                                                            | `c0597b1`; latest run `audit_289de0bc91af1144`, 49 repositories matched, fleet mean `1.719 / 4`             |
| 2026-07-26 | Added an explicit confirmation and in-progress state before the portfolio-wide local refresh, including scope, duration warning, cancellation, and read-only boundaries.                                  | `7e734f1`; full tests, build, install, and commit-time quality gates passed                                 |
| 2026-07-26 | Moved the read-only local refresh scan onto Tauri's blocking worker pool so a portfolio-wide scan no longer occupies the UI thread.                                                                       | `fb5ea92`; 32 Rust tests, full build, install, and commit-time quality gates passed                         |
| 2026-07-26 | Simplified the fleet quality matrix to the six canonical gates; discovered custom gates no longer widen every repository row.                                                                             | `88e1a83`; 32 Rust tests, 5 Vitest tests, full build, install, and commit-time quality gates passed         |
| 2026-07-26 | Expanded the canonical taxonomy with Tests and Secrets scan, normalized existing aliases, made Dependency audit conditional, and hid absent optional/custom matrix cells.                                 | `c4e9a46`; 32 Rust tests, 5 Vitest tests, full build, install, and commit-time quality gates passed         |
| 2026-07-26 | Recorded a 57-project quality-gate recommendation matrix with baseline/conditional taxonomy, alias normalization, raw-label cleanup, and unavailable-path safeguards.                                     | `4c73ee5`; 44 available paths, 13 unavailable, four fixture candidates; docs gates passed                   |
| 2026-07-26 | Added a separate CI-readiness `/4` score with exact per-repository gate gaps and fleet counts; imported maturity remains unchanged.                                                                       | `360faf2`; 36 Rust tests, web gates, `cargo fmt --check`, `git diff --check`, and full app/DMG build passed |
| 2026-07-26 | Fixed stale local repository retention and added a prominent command-center maturity/readiness summary.                                                                                                   | `05948ec`; 44 current repositories, zero excluded paths, full build/install, live refresh passed            |
